# Enable disk cache (local)
build --disk_cache=~/.bazel-cache
test  --disk_cache=~/.bazel-cache

# Remote cache (example – commented for safety)
# build --remote_cache=http://your-remote-cache:8080
# test  --remote_cache=http://your-remote-cache:8080

# Improve CI stability
build --keep_going
test  --test_output=errors

